name: icons_launcher_example
description: A new example flutter project to quickly test icons launcher.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  icons_launcher:
    path: ../

flutter:

  uses-material-design: true
